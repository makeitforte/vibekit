import { PlannerShell } from "@/project_timeline_and_capacity_planner/planner-shell";

export const metadata = {
  title: "Project Timeline & Capacity — VibeKit",
};

export default function ProjectPlannerPage() {
  return <PlannerShell />;
}
